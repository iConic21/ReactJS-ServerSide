import React from 'react';

const Home = () => {
    return (
        <div>
            <div>I'm the best test home component</div>
            <button onClick={() => console.log('hello')} >Press me!</button>
        </div>
    )
};

export default Home;